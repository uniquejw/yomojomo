<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bts.yomojomo.dao.BoardDao">
  
  <!-- 테이블의 컬럼과 객체필드를 연결한다. -->
  <resultMap type="Board" id="BoardMap">
    <id column="b_no" property="no"/>
    <result column="g_no" property="gno"/>
    <result column="memb_no" property="membno"/>
    <result column="reg_dt" property="regdt"/>
    <result column="content" property="content"/>
    <result column="view_cnt" property="viewcnt"/>
    <result column="status" property="status"/>
  </resultMap>

  <select id="findAll" resultMap="BoardMap"> 
    select
      b_no,
      g_no,
      memb_no,
      reg_dt,
      content,
      view_cnt,
      status
    from
      gms_board
    order by
      b_no desc
  </select>
  
  <select id="findByNo" resultMap="BoardMap" parameterType="int">
    select 
      b_no,
      g_no,
      memb_no,
      reg_dt,
      content,
      view_cnt,
      status
    from 
      gms_board
    where 
      b_no=#{no}
  </select>

  <insert id="insert" parameterType="Board">
    insert into gms_board(g_no, memb_no,reg _dt, content, view_cnt, status) 
    values(#{gno}, #{membno}, #{regdt}, #{content}, #{viewcnt}, #{status})
  </insert>
  
  <update id ="update" parameterType="Board">
    update gms_board set 
      g_no = #{gno},
      memb_no = #{membno},
      reg_dt = #{regdt},
      content = #{content},
      view_cnt = #{viewcnt},
      status = #{status}
    where 
      b_no = #{no}
  </update>
  
  <select id ="countAll" parameterType="int">
    select count(*) from gms_board
  </select>
  
  <delete id="delete" parameterType="int">
    delete from gms_board
    where 
      b_no = #{no}
  </delete>
</mapper>