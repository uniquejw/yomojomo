<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bts.yomojomo.dao.RecruitDao">
  
  <!-- 테이블의 컬럼과 객체필드를 연결한다. -->
  <resultMap type="Recruit" id="RcruitMap">
    <id column="recruit" property="no"/>
    <result column="title" property="title"/>
    <result column="content" property="content"/>
    <result column="reddt" property="reddt"/>
    <result column="viewcnt" property="viewCnt"/>
    <result column="itrlocalno" property="itrLocalNo"/>
    <result column="fileno" property="fileNo"/>
    <result column="membno" property="membNo"/>
  </resultMap>

  <select id="findAll" resultMap="RcruitMap"> 
    select
      recruit,
      title,
      content,
      reddt,
      viewcnt,
      itrlocalno,
      fileno,
      membno
    from
      gms_recruit
    order by
      recruit desc
  </select>
  
  <select id="findByNo" resultMap="RcruitMap" parameterType="int">
    select 
      recruit,
      title,
      content,
      reddt,
      viewcnt,
      itrlocalno,
      fileno,
      membno
    from 
      gms_recruit
    where 
      recruit=#{no}
  </select>

  <insert id="insert" parameterType="Recruit">
    insert into gms_recruit(title, content, reddt, viewcnt, itrlocalno, fileno, membno) 
    values(#{title}, #{content},"2022-03-16",0, 1, 1 ,1)
  </insert>
  
  <update id ="update" parameterType="Recruit">
    update gms_recruit set 
      title = #{title},
      content = #{content},
      reddt = #{reddt},
      viewcnt = #{viewCnt},
      itrlocalno = itrlocalno,
      fileno = fileno,
      membno =membno
    where 
      recruit = #{no}
  </update>
  
  <select id ="countAll" parameterType="int">
    select count(*) from gms_recruit
  </select>
  
  <delete id="delete" parameterType="int">
    delete from gms_recruit
    where 
      recruit = #{no}
  </delete>
  
  <update id="increaseViewCount" parameterType="int">
    update gms_recruit set
      viewcnt = viewcnt + 1
    where
      recruit = #{no}
  </update>
  

    
    
    
</mapper>